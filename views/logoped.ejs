<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="./logoped.css" rel="stylesheet" />
  <script src="./therapist.js" type="module"></script>
  <title>Logoped</title>
  <link rel="stylesheet" href="./css/bootstrap.min.css" />
</head>
<body class="text-center">

  <div id="therapist-app">
    <button id="sign-out-btn" class="btn btn-danger mb-3 mt-3">
      Închide sesiunea
    </button>
    <div>
      Bine ai revenit, <%= fullname %>.
    </div>
    <div id="therapist-app white-bg">
      <h1 class="mt-3">Clase</h1>
        <ul class="list-group list-group-flush text-left" id="classrooms-list">
          <% classrooms.forEach((classroom) => { %>
            <li class="list-group-item">
              <div style="float: left;">
                <a href="<%= classroom.link %>" id="<%= 'classroom-link-' + classroom.id %>">
                  <%= classroom.name %>
                </a>
                <input type="text" class="form-control d-none edit-classroom-title" id="<%= 'classroom-edit-' + classroom.id %>" value="<%= classroom.name %>">
              </div>
              <div class="edit-classroom-link" style="float: right;" data-id="<%=classroom.id%>" id="<%= 'edit-' + classroom.id %>">
                <span class="p-2 badge bg-primary rounded-pill see-results">
                  Editează
                </span>
              </div>
              <div class="save-classroom-link d-none" style="float: right;" id="<%= 'save-' + classroom.id %>" data-id="<%=classroom.id%>">
                <span class="p-2 badge bg-success rounded-pill see-results">
                  Salvează
                </span>
              </div>
            </li>
          <% }) %>
        </ul>
          <div class="mt-3">
            <p>Pentru a adăuga o clasă, introduceți mai jos numele acesteia.</p>
            <div class="input-group mb-3">
              <input type="text" class="form-control" id="add-classroom-input" placeholder="Numele clasei" aria-label="Recipient's username" aria-describedby="basic-addon2">
              <div class="input-group-append">
                <button class="btn btn-primary" id="add-classroom-btn">Adaugă Clasă</button>
              </div>
            </div>
            <div class="error text-danger mt-3"></div>
          </div>
        </div>
      </div>
    </div>
</body>
</html>
